class Canvas:
    def __init__(self, width, height):
        # Initialize the canvas with the specified width and height
        pass
    def draw_pixel(self, x, y, color):
        # Draw a pixel on the canvas at the given coordinates with the specified color
        pass
    def draw_line(self, x1, y1, x2, y2, color):
        # Draw a line on the canvas between the given coordinates with the specified color
        pass
    def draw_circle(self, x, y, radius, color):
        # Draw a circle on the canvas with the given center coordinates, radius, and color
        pass